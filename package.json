{
  "dependencies": {
    "axios": "^0.22.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "build": "npm i && cd ./serverless && npm i",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^27.2.5"
  }
}